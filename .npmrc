# Electron 二进制不走 GitHub Releases —— 本机实测 github.com/electron/electron/releases
# 直连 20s 超时，而 npmmirror 可达。@electron/get 会读这个配置去取 dist zip。
# 只影响 Electron 二进制下载，不影响 npm 包本身（仍走 registry.npmjs.org）。
electron_mirror=https://npmmirror.com/mirrors/electron/
