node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/
*.tsbuildinfo

# 本地数据（应用运行时会写到 userData，不会落在这里，但以防万一）
*.db
*.db-wal
*.db-shm
blobs/

# M5 探针的证据归档**要进版本库**（它是文档里那些数字的唯一来源），
# 所以这里显式写一条否定的意图说明：`out/` 那条规则**不覆盖** `scripts/evidence/`。
# 若日后要丢弃它们，请改这一行，而不是让 `out/` 那条规则顺手替你做决定。
