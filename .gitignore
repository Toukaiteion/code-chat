node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/
*.tsbuildinfo

# 本地数据（应用运行时会写到 userData，不会落在这里，但以防万一）
*.db
*.db-wal
*.db-shm
blobs/

# M5 探针的证据归档**要进版本库**（它是文档里那些数字的唯一来源）。
# 上面 `out/` 那条规则本来就不覆盖 `scripts/evidence/`；真正的漏网之鱼是第 5 行的
# `*.log` —— 它会**静默吃掉**归档里的 `console.log` 与 `app-*.log`。
# 报告不读日志，所以既有结论不受影响；但「证据完整」这半边一直是破的，
# 而一条安静地少收证据的规则与一条正常的规则长得一模一样 ——
# 所以它从 M5 一直藏到 M7a 才发现（`design.md:2535-2541` 逐字警告过这一条）。
# 下面这条否定规则把它补上。若日后要丢弃这些归档，请改这里，
# 而不是让上面的通配规则顺手替你做决定。
!scripts/evidence/**/*.log
